/* npm install jshint -g */

/* Para utilizar jshint NomeArquivo 
  Adicionar configurações personalizadas, criar um arquivo config.json
  e chamar no terminal jshint NomeArquivo --config ./config.json
*/

function main(a, b) {
  if (b) {
    return a === null;
  }
}